# buildSrc/src/main/java/

## Responsibility
Java package root for buildSrc convention plugin code.

## Design
Namespace aggregator for `co.anitrend.support.markdown.buildSrc` build logic.

## Flow
Routes Gradle plugin implementation readers into the package hierarchy.

## Integration
Child map: `buildSrc/src/main/java/co/codemap.md`.
